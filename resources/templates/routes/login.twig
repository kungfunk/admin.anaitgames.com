{% extends '/shared/base.twig' %}

{% block title %}Login{% endblock %}

{% block body %}
    <div class="login">
        <div class="box">
            <div class="box__content">
                <form action="{{ path_for('login') }}" method="post" class="form">
                    {{ include('shared/messages.twig') }}
                    <div class="form__group-list">
                        <div class="form__group">
                            <input name="username" type="text" required class="form__input" placeholder="Usuario" value="{{ previousInput.username }}">
                        </div>
                        <div class="form__group">
                            <input name="password" type="password" required class="form__input" placeholder="Password" value="{{ previousInput.password }}">
                        </div>
                        <button class="button button--big button--full-width button--primary" type="submit">Login</button>
                    </div>

                    {{ csrf | raw }}
                </form>
            </div>
        </div>
    </div>
{% endblock %}
